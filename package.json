{
    "name": "civis-analysis2",
    "version": "0.1.0",
    "private": true,
    "description": "Legacy jQuery/D3 static site with live reload dev server and static build.",
    "scripts": {
        "dev": "live-server --port=3000 --watch=. --wait=200 --ignore=dist,node_modules,.git --no-browser",
        "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
        "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && cpy \"{index.html,images/**,javascripts/**,stylesheets/**,fonts/**,data/**}\" dist --parents",
        "preview": "serve dist -l 3000",
        "test": "node scripts/verify-approval-rule.js && node scripts/verify-government-rule.js && node scripts/verify-alignment-scale.js && node scripts/verify-context-menus.js && node scripts/verify-bar-chart.js && node scripts/verify-layer-panel.js && node scripts/verify-derived-layers.js && node scripts/verify-vote-agreement.js"
    },
    "devDependencies": {
        "cpy-cli": "^5.0.0",
        "jsdom": "^29.1.1",
        "live-server": "^1.2.2",
        "serve": "^14.2.3"
    },
    "engines": {
        "node": ">=18"
    }
}
